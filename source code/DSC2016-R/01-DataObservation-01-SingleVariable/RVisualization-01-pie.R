# ??ˆå»ºç«‹sex?‰©ä»?
sex <- table(hsb$sex)

# ??è¨­???
pie(sex)

# ?? ä?Šæ?™é??
pie(sex, main = "Sex")

# ?”¹é¡è‰²
col <- rainbow(length(sex)) # ?ˆ©?”¨rainbow?‡½?•¸?”¢??Ÿè‹¥å¹²ç¨®ä¸å?Œç?„è‰²ç³?
col # ?€™æ˜¯RGB??„å€?
pie(sex, main = "Sex", col = col)

# ?? ä?Šæ?”ç??
pct <- sex / sum(sex) * 100 # è¨ˆç?—ç™¾??†æ??
label <- paste0(names(sex), " ", pct, "%") # ?”¢??Ÿèªª??Žæ?‡å??
label # ??Œå­¸?¯ä»¥æ?”è?ƒlabel, names(sex), pct ??? "%" ??„ç?æ??
pie(sex, labels = label)

if (FALSE) {
  # 3D pie chart
  library(plotrix) # ??Œå­¸è«‹è‡ªè¡Œå?‰è?é€™å€‹å?—ä»¶
  pie3D(sex, labels = label)
}

# è«‹å?žåˆ°consoleè¼¸å…¥`submit()`

